/**
 * Controls the botmux self-update feature.
 *
 * The project no longer follows the upstream npm release line. Keep every
 * self-update entry point disabled until a fork-specific update source and
 * delivery flow are implemented.
 */
export const BOTMUX_UPDATE_FEATURE_ENABLED = false;
